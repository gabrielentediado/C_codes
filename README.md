Códigos em linguagem C para estudos.

O programa "app" é um simples programa, consegue executar um listador de nomes e uma calculadora.
    - Usa os operadores if e else
    - O switch

Por equanto é só isso.

Bons estudos, dev. 